## HydroGrid Backend Configuration
# Copy to `.env` inside `backend/` and fill values.

# Security
JWT_SECRET=change-this-in-production
IOT_INGEST_KEY=dev-iot-key

# Twilio SMS (optional)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=

# Gemini (optional)
GEMINI_API_KEY=

